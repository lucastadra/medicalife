export interface Patient {
    id: string;
    name: string;
    email: string;
    birthDate: string;
    createdAt: string;
    updatedAt: string;
    postalCode: string;
    streetAddress: string;
    city: string;
    state: string;
}
